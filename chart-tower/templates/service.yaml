apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-tower.fullname" . }}
  labels:
    {{- include "chart-tower.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-tower.selectorLabels" . | nindent 4 }}
