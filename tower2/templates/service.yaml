apiVersion: v1
kind: Service
metadata:
  name: {{ include "tower2.fullname" . }}
  labels:
    {{- include "tower2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tower2.selectorLabels" . | nindent 4 }}
