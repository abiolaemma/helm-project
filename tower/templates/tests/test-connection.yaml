apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tower.fullname" . }}-test-connection"
  labels:
    {{- include "tower.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tower.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
