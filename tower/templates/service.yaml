apiVersion: v1
kind: Service
metadata:
  name: {{ include "tower.fullname" . }}
  labels:
    {{- include "tower.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tower.selectorLabels" . | nindent 4 }}
