apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tower-chart.fullname" . }}-test-connection"
  labels:
    {{- include "tower-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tower-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
